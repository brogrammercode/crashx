name: crashx
description: A new Flutter plugin for CrashX analytics.
version: 0.0.1
homepage: https://github.com/brogrammercode/crashx

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  device_info_plus: ^11.1.0
  package_info_plus: ^8.1.0
  uuid: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
